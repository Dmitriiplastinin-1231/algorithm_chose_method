# Методы численного интегрирования для моделирования динамики ракеты / траектории

## Обзор

При моделировании полёта ракеты система уравнений движения представляет собой задачу Коши для системы ОДУ:

$$\dot{\mathbf{x}} = f(t, \mathbf{x}, \mathbf{u})$$

где **x** — вектор состояния (координаты, скорости, углы, угловые скорости), **u** — управление (отклонение сопла и т.д.).  
Выбор численного метода определяется требуемой точностью, жёсткостью системы, наличием запаздывания и ограничениями по вычислительным ресурсам.

---

## Сводная таблица методов

| Метод | Порядок точности *p* | Тип | Адаптивный шаг | Когда выбирать |
|---|:---:|---|:---:|---|
| **Эйлер (Forward Euler)** | 1 | явный, одношаговый | нет | Быстрая черновая оценка; непригоден для точной траектории |
| **Улучшенный Эйлер (Heun)** | 2 | явный, одношаговый | нет | Простые оценки в Excel/на бумаге; в 2 раза точнее Эйлера |
| **Midpoint / RK2** | 2 | явный, одношаговый | нет | Базовый уровень точности при минимуме вычислений |
| **RK3** | 3 | явный, одношаговый | нет | Промежуточный вариант; редко используется отдельно |
| **Классический RK4** | 4 | явный, одношаговый | нет | **Стандарт "по умолчанию"** для траекторий ракет; балансирует точность и стоимость |
| **RKF45 / Dormand–Prince (ode45)** | 4(5) / 5 | явный, одношаговый | **да** | Адаптивный контроль шага; удобен при переменной динамике и событиях (отделение ступени) |
| **Adams–Bashforth (ABk)** | *k* (1…8) | явный, многошаговый | нет | Экономичен при гладких решениях; требует истории *k* предыдущих шагов |
| **Adams–Moulton (AMk) / предиктор–корректор** | *k*+1 (2…8) | неявный (PC), многошаговый | нет | Выше точность при той же стоимости, что AB; подходит для средней жёсткости |
| **Backward Euler** | 1 | неявный, одношаговый | нет | Очень жёсткие системы; первый порядок — только при крайней нужде |
| **Trapezoid / Crank–Nicolson** | 2 | неявный, одношаговый | нет | Устойчивость + 2-й порядок; хорош для линейных/слабо нелинейных жёстких систем |
| **BDF / ode15s (MATLAB)** | 1–6 | неявный, многошаговый | **да** | **Стандарт для жёстких систем** (TVC-привод, фильтры, упругие колебания конструкции) |
| **Symplectic Euler** | 1 | симплектический, одношаговый | нет | Сохраняет гамильтонову структуру; длительное орбитальное моделирование |
| **Verlet / Leapfrog** | 2 | симплектический, одношаговый | нет | Орбитальная механика; лучше сохраняет энергию на длинных интервалах, чем обычный RK2 |

> **Порядок p** — порядок по шагу h для **глобальной** ошибки: O(hᵖ).  
> Локальная ошибка за один шаг обычно на 1 выше: O(hᵖ⁺¹).

---

## Пояснения по каждому методу

### 1. Эйлер (явный, 1-й порядок)

$$\mathbf{x}_{n+1} = \mathbf{x}_n + h\,f(t_n, \mathbf{x}_n)$$

Простейший метод. Требует очень мелкого шага для приемлемой точности. Для реалистичных траекторий ракеты практически неприменим как основной метод — используется лишь для иллюстраций и отладки.

---

### 2. Улучшенный Эйлер / Heun (явный, 2-й порядок)

$$k_1 = f(t_n, \mathbf{x}_n),\quad k_2 = f(t_n+h,\, \mathbf{x}_n + h k_1)$$
$$\mathbf{x}_{n+1} = \mathbf{x}_n + \tfrac{h}{2}(k_1 + k_2)$$

Позволяет сделать расчёт "в одну таблицу" в Excel. Для оценки порядка смещения траектории достаточно.

---

### 3. Midpoint / RK2 (явный, 2-й порядок)

$$k_1 = f(t_n, \mathbf{x}_n),\quad k_2 = f\!\left(t_n+\tfrac{h}{2},\, \mathbf{x}_n + \tfrac{h}{2} k_1\right)$$
$$\mathbf{x}_{n+1} = \mathbf{x}_n + h\,k_2$$

Аналог Heun, использует значение в середине шага.

---

### 4. Классический RK4 (явный, 4-й порядок)

$$k_1 = f(t_n, \mathbf{x}_n),\quad k_2 = f\!\left(t_n+\tfrac{h}{2},\, \mathbf{x}_n+\tfrac{h}{2}k_1\right)$$
$$k_3 = f\!\left(t_n+\tfrac{h}{2},\, \mathbf{x}_n+\tfrac{h}{2}k_2\right),\quad k_4 = f(t_n+h,\, \mathbf{x}_n+h\,k_3)$$
$$\mathbf{x}_{n+1} = \mathbf{x}_n + \tfrac{h}{6}(k_1 + 2k_2 + 2k_3 + k_4)$$

**Рабочая лошадка** баллистики и динамики полёта. 4 вычисления правой части на шаг — отличная точность при умеренной стоимости. Стандартный выбор для моделирования траектории ракеты с фиксированным шагом.

---

### 5. RKF45 / Dormand–Prince (ode45) (явный, адаптивный, порядок 4/5)

Использует 6–7 вычислений правой части для одновременного получения решения 4-го и 5-го порядков. Разность — оценка локальной ошибки, по которой автоматически выбирается следующий шаг h. В MATLAB реализован как `ode45`.

**Применение:** практически любые нежёсткие задачи динамики полёта, события (отделение ступени, включение двигателя), переменная динамика по участкам полёта.

---

### 6. Adams–Bashforth / Adams–Moulton (многошаговые)

Используют *k* предыдущих точек. Экономичны (1 вычисление правой части на шаг при использовании схемы предиктор–корректор), но:
- требуют хранения истории решения,
- сложнее обрабатывают события и разрывы (отделение ступени, удары),
- нуждаются в "запуске" одношаговым методом.

Порядок AB4 = 4, AM4 = 5 (при 4 предыдущих шагах).

---

### 7. BDF / ode15s (неявный, адаптивный, порядки 1–6)

Backward Differentiation Formulas — неявные многошаговые методы. Каждый шаг требует решения системы нелинейных уравнений (итерации Ньютона). Реализован в MATLAB как `ode15s`.

**Применение:** *жёсткие* системы — модели с сильно различающимися характерными временами (приводы TVC, гидравлика, фильтры, упругие моды конструкции ракеты). Если ode45 вынужден брать очень мелкий шаг из-за "жёсткости" — нужен BDF.

---

### 8. Симплектические методы (Verlet, Leapfrog)

Сохраняют симплектическую структуру гамильтоновой механики → практически не накапливают ошибку по энергии на длинных промежутках. Незаменимы при моделировании орбитального участка (от отделения ступени до выхода на орбиту). Verlet имеет 2-й порядок точности, но энергетически гораздо лучше обычного RK2 на длинных траекториях.

---

## Рекомендации для задач с запаздыванием и управлением

### Задержка в контуре управления (τ = 0.3 с и подобные)

Задержка реализуется не выбором интегратора, а **буфером истории**: на каждом шаге управление берётся из буфера, соответствующего моменту $t - \tau$:

$$\mathbf{u}(t) = \mathbf{u}_{\text{buf}}(t - \tau)$$

Интегратор (RK4, ode45 и т.д.) работает с этим буфером прозрачно. Важно:
- шаг интегрирования **h** ≪ τ (рекомендуется h ≤ τ/10),
- при адаптивном шаге (ode45) нужно обеспечить, чтобы h ≤ τ/5 как минимум.

### Выбор метода по ситуации

| Ситуация | Рекомендуемый метод |
|---|---|
| Оценка "на бумаге"/Excel | Улучшенный Эйлер (Heun), 2-й пор. |
| Стандартное моделирование траектории (нежёсткое) | **RK4** (4-й пор.) или **ode45** (адаптивный 5-й) |
| Переменная динамика, события (отделение, включение) | **ode45 / Dormand–Prince** |
| Жёсткие системы (TVC-привод, фильтры, упругие колебания) | **BDF / ode15s** (порядки 1–6) |
| Длинные орбитальные дуги, сохранение энергии | **Verlet / Leapfrog** (симплектический, 2-й пор.) |
| Гладкое решение, нужна максимальная скорость | **Adams–Bashforth 4 / предиктор–корректор** |
| Задача с задержкой τ в управлении | RK4 или ode45 + буфер истории; h ≪ τ |

---

## Связь с задачей морского старта (пример)

Для задачи "морская стартовая платформа — качка 8 c, амплитуда 2°, задержка 0.3 c, оценка смещения на 10 км":

1. **Инженерная оценка (без симулятора):** линеаризация малых возмущений (уравнение Δy ≈ H·θ_err) + аналитический учёт задержки через фазовый сдвиг φ = ω·τ.
2. **Численная проверка:** RK4 или ode45 с буфером задержки для нескольких ключевых фаз качки.
3. **Статистическая оценка (Монте–Карло):** прогон линеаризованной модели при случайной фазе старта ∈ U(0, 2π); 500–2000 прогонов дают 3σ и P(|Δy| > 1 км).
4. **Вывод:** смещение порядка 80–200 м ≪ ±1 км → требование точности выполняется.

---

## Источники

- Greensite A.L. *Analysis and Design of Space Vehicle Flight Control Systems.* 1955 (линеаризация и малые возмущения).
- Blakelock J.H. *Automatic Control of Aircraft and Missiles.* (передаточные функции, TVC).
- Smith O.J.M. *Closer control of loops with dead time.* Chemical Engineering Progress, 1957 (задержка в контуре управления).
- Dormand J.R., Prince P.J. *A family of embedded Runge–Kutta formulae.* J. Comput. Appl. Math., 1980.
- Hairer E., Nørsett S.P., Wanner G. *Solving Ordinary Differential Equations I & II.* Springer.
