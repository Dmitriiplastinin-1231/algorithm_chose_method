# Рекомендация: численный метод интегрирования порядка ≤ 2 для задачи морского старта

## Постановка задачи (краткое напоминание)

| Параметр | Значение |
|---|---|
| Качка платформы | период T = 8 с, амплитуда A ≤ 2° (0.0349 рад) |
| Задержка управления | τ = 0.3 с |
| Высота оценки отклонения | H = 10 000 м |
| Требование точности | ±1 000 м |

---

## Почему нужен метод порядка не выше 2

Преподаватель обоснованно указал, что **методы Рунге–Кутты 4-го порядка (RK4) избыточно сложны** для данной задачи:

- уравнение движения ракеты при малых возмущениях *уже линеаризовано* и сводится к
  уравнению **второго порядка**:

  $$\ddot{y} = f(t, y, \dot{y})$$

- глобальная ошибка порядка *O(h²)* вполне достаточна при шаге h = 0.1…0.5 с для
  оценки отклонения на 10 км;
- методы 2-го порядка **полностью реализуются в Excel** (табличный расчёт, без кода),
  что важно для учебного формата.

---

## Рекомендованный метод

### Вариант 1 — Velocity-Verlet (центральные разности) для уравнений 2-го порядка

Это **лучший компромисс** для данной задачи:

| Критерий | Оценка |
|---|---|
| Порядок глобальной ошибки | O(h²) |
| Уравнения | работает **напрямую со 2-м порядком**, без понижения |
| Вычислительная сложность | 1 вычисление ускорения на шаг |
| Устойчивость | хорошая для механических систем |
| Реализация в Excel | ✅ тривиальная (3 столбца: t, y, ẏ) |
| Сохраняет структуру задачи | ✅ (симплектический по природе) |

**Формулы шага** (для уравнения движения $\ddot{y} = a(t, y, \dot{y})$):

$$y_{n+1} = y_n + \dot{y}_n \, h + \tfrac{1}{2} a_n \, h^2$$

$$\dot{y}_{n+1} = \dot{y}_n + \tfrac{1}{2}(a_n + a_{n+1}) \, h$$

где $a_n = a(t_n, y_n, \dot{y}_n)$ — боковое ускорение (от остаточного угла наклона).

---

### Вариант 2 — Метод Хойна (Heun / RK2) для системы 1-го порядка

Если задачу удобнее записать через систему первого порядка
$\dot{z} = F(t, z)$, где $z = (y, \dot{y})^\top$:

| Критерий | Оценка |
|---|---|
| Порядок глобальной ошибки | O(h²) |
| Число вычислений F на шаг | 2 (предиктор + корректор) |
| Реализация в Excel | ✅ (6 столбцов на шаг) |

**Формулы шага:**

$$\tilde{z}_{n+1} = z_n + h \, F(t_n, z_n) \quad \text{(предиктор)}$$

$$z_{n+1} = z_n + \tfrac{h}{2}\bigl[F(t_n, z_n) + F(t_{n+1}, \tilde{z}_{n+1})\bigr] \quad \text{(корректор)}$$

---

## Модель уравнения движения (шаблон для обоих методов)

### Уравнение бокового движения (линеаризованное)

Остаточный угол ошибки после компенсации с задержкой τ:

$$\theta_\mathrm{err}(t) = A \sin(\omega t_0 + \varphi_0) \cdot \sin(\omega \tau)$$

где:
- $\omega = 2\pi / T = 2\pi / 8 \approx 0.785$ рад/с
- $t_0$ — момент старта
- $\varphi_0$ — фаза качки в момент старта (случайный параметр для Монте–Карло)

Боковое ускорение от некомпенсированного наклона (при малых углах):

$$a(t) \approx g \cdot \theta_\mathrm{err}(t) \approx 9.81 \cdot \theta_\mathrm{err}(t) \quad [\text{м/с}^2]$$

> В более точной модели $g$ заменяется на проекцию тяги: $a = (F_\mathrm{thr}/m) \cdot \theta_\mathrm{err}$.
> Для оценки порядка величины — $g$ вполне достаточно.

Начальные условия: $y(0) = 0$, $\dot{y}(0) = 0$ (нет начальной боковой скорости).

---

## Алгоритм расчёта в Excel (таблица)

Шаг по времени: **h = 0.1 с**  
Число шагов до H = 10 000 м: зависит от профиля скорости; для оценки можно принять
постоянную вертикальную скорость $v_z \approx 300$ м/с → время подъёма $t_f \approx 33$ с
→ **N ≈ 330 шагов**.

| Столбец | Формула |
|---|---|
| A: $t_n$ | A1 = 0; A(n+1) = A(n) + h |
| B: $\theta_\mathrm{err}(t)$ | = A · SIN(ω·A(n) + φ₀) · SIN(ω·τ) |
| C: $a_n$ | = g · B(n) |
| D: $y_n$ | D1 = 0; D(n+1) = D(n) + E(n)·h + 0.5·C(n)·h² |
| E: $\dot{y}_n$ | E1 = 0; E(n+1) = E(n) + 0.5·(C(n) + C(n+1))·h |

Результат: $\Delta y = D(N)$ — боковое смещение на высоте 10 км.

---

## Численный пример (худший случай)

### Параметры
- A = 0.0349 рад (2°), T = 8 с, τ = 0.3 с
- $\omega\tau = 0.785 \cdot 0.3 \approx 0.236$ рад
- $\theta_\mathrm{err} = 0.0349 \cdot \sin(0.236) \approx 0.0349 \cdot 0.234 \approx 0.00816$ рад

### Результат (геометрическая оценка)
$$\Delta y \approx H \cdot \theta_\mathrm{err} = 10\,000 \cdot 0.00816 \approx \mathbf{82 \text{ м}}$$

Числовое интегрирование методом Velocity-Verlet с h = 0.1 с даёт близкий результат
(~80–90 м в зависимости от фазы φ₀).

### Вывод
$$82 \text{ м} \ll 1000 \text{ м}$$

**Смещением можно пренебречь** при требовании точности ±1 км.

---

## Сравнение методов по критериям задачи

| Метод | Порядок | Excel | Уравнения | Примечание |
|---|:---:|:---:|---|---|
| Эйлер (явный) | 1 | ✅ | 1-й или 2-й | Грубо; требует малый h |
| **Velocity-Verlet** | **2** | **✅** | **напрямую 2-й** | **Рекомендуется** |
| **Хойн (RK2 / Heun)** | **2** | **✅** | **система 1-го** | **Рекомендуется** |
| Метод Ньюмарка-β (β=1/4) | 2 | ✅ | напрямую 2-й | Неявный; устойчив для жёстких систем |
| RK4 | 4 | ⚠️ | система 1-го | Избыточно сложен для данной задачи |

---

## Итоговая рекомендация

> **Используйте Velocity-Verlet (или Heun/RK2) — оба метода второго порядка,
> полностью реализуются в Excel, работают с уравнением второго порядка (или
> эквивалентной системой первого порядка), и дают точность O(h²), достаточную
> для оценки смещения порядка десятков–сотен метров при допуске ±1 км.**

**Обоснование выбора:**
1. Уравнение движения — 2-й порядок → метод 2-го порядка органичен и не требует
   лишних промежуточных вычислений.
2. Ошибка ~O(h²) при h = 0.1 с: глобальная ошибка порядка 1% от результата —
   намного меньше, чем требование ±1 км.
3. Velocity-Verlet является **симплектическим** — хорошо сохраняет интегралы движения
   (энергию/импульс) при длительном интегрировании механических систем.
4. Метод Хойна (Heun) — предиктор-корректор — нагляден педагогически и совпадает
   с явной формулой трапеций.
5. Оба метода значительно проще RK4 и при этом дают существенно лучшую точность,
   чем метод Эйлера 1-го порядка.

---

## Источники

Нумерация соответствует библиографическому списку в файле `Analiz_primenimosti_linearizacii_v_zadachah_upravleniya_raketnymi.pptx` данного репозитория.

| № | Автор, год | Полное название | Применение в данном документе |
|---|---|---|---|
| [1] | Bryson & Ho (1975) | *Applied Optimal Control*, Hemisphere | Общий фундамент теории управления |
| [3] | Blakelock (1991) | *Automatic Control of Aircraft and Missiles*, Wiley | Динамика полёта, уравнения движения |
| [5] | — | Теория малых возмущений (основы) | Линеаризация уравнений движения |
| [6] | Greensite (1970) | *Analysis and Design of Space Vehicle Flight Control Systems*, Spartan | Метод малых возмущений для ракет |
| [8] | Smith (1959) | «Closer Control of Loops with Dead Time», *Chem. Eng. Prog.* 53(5) | Учёт задержки управления как фазового сдвига |
| — | Störmer–Verlet / Leapfrog | Классика численных методов механики (см. любой учебник численного анализа) | Velocity-Verlet, порядок 2 |
| — | Heun (1900) | «Neue Methode zur approximativen Integration», *Z. Math. Phys.* 45 | Метод Хойна (RK2/Heun), порядок 2 |
